In what scenario does Quicksort experience its worst-case
      time complexity? You may assume that we always pick the first element as
      the pivot.

      \begin{answer}
      Data that is (nearly) sorted or is (nearly) sorted in reverse order.
      \end{answer}