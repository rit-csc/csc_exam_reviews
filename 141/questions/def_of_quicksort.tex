What sorting algorithm splits its input list into two other lists,
	one which has all elements that are smaller than a given pivot value
	and one which has elements that are larger than the pivot value?

    \begin{answer}
    Quicksort.
    \end{answer}